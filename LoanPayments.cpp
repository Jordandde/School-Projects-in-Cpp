#include <iostream>
#include <stdlib.h>
using namespace std;
int main(const int argc, const char*const argv[]) {
	float d = atof(argv[1]);
	float p = atof(argv[2]);
	float i = atof(argv[3]);
	float epsilon = 0.0001f;
	float dNew = d;
	while(dNew > 0){
		
	}